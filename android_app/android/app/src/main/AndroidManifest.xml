<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Lets the app ask, once, to be exempted from Doze/App-Standby battery
         throttling (requested at boot in main.dart, same pattern as the
         storage/notification prompts below). Some OEM skins (this was found
         on a ColorOS/Oppo device) freeze an app's process for a few seconds
         under normal background-management heuristics even while it holds a
         WAKE_LOCK; if that freeze lands while Android is mid-dispatch of a
         touch event (e.g. the tap that opens a movie), the freeze itself is
         indistinguishable from a hang and the system throws an ANR ("Input
         dispatching timed out") and kills the app — which is what showed up
         as a white screen followed by the app restarting. Being on the
         exemption list is the standard fix OEM-skin power management
         actually respects; it doesn't stop the OS from ever freezing the
         process, but makes it far less likely to happen to a foreground app
         mid-gesture. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- Android 13+ requires this to actually show a notification (the
         announcement push) — requested at runtime in AppNotifications.init,
         called once at app boot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Only meaningful on Android 12 and below — downloads already save to
         this app's own scoped-storage folder, which needs no permission on
         newer Android at all. Requested at runtime before the first
         download (DownloadManager._ensureStoragePermission) so it still
         shows the expected prompt on older devices. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <!-- Without these two, the Play Store (and some TV launchers) treat the
         app as phone-only and won't offer/allow install on an Android TV
         device at all — a touchscreen and leanback (TV) support are both
         declared optional so the same APK installs on either. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <application
        android:label="MY IPTV"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
